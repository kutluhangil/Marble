/**
 * Shared cloud rotation, written by the cloud mesh and read by the Earth shader
 * so cloud shadows line up with the (separately rotating) cloud layer.
 */
export const cloudState = { rotation: 0 };
